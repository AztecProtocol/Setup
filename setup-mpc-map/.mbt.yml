name: setup-mpc-map
build:
  default:
    cmd: ../ci-scripts/build.sh
    args:
      - setup-mpc-map
commands:
  deploy:
    cmd: ../ci-scripts/deploy.sh
    args:
      - setup-mpc-map
dependencies:
  - setup-mpc-common