name: setup-post-process
build:
  default:
    cmd: ../ci-scripts/build.sh
    args:
      - setup-post-process
commands:
  deploy:
    cmd: ../ci-scripts/deploy.sh
    args:
      - setup-post-process
dependencies:
  - setup-tools
