name: setup-post-process
build:
  default:
    cmd: ../ci-scripts/build.sh
    args:
      - setup-post-process
      - 278380418400.dkr.ecr.us-east-2.amazonaws.com
commands:
  deploy:
    cmd: ../ci-scripts/deploy.sh
    args:
      - setup-post-process
      - '-'
      - us-east-2
dependencies:
  - setup-tools
